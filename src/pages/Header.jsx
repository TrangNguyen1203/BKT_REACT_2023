import React from 'react';

const Header = () => {
    return (
        <div style={{padding:"16px 0", border: "1px solid #ccc", color: "black"}}>
            <h1 style={{marginTop: "20px"}}>HIT BLOG</h1>
        </div>
    );
};

export default Header;